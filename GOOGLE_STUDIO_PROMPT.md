# Prompt for Google Studio

Open the existing local VIGIL workspace and continue the current Expo/React Native implementation; do not start a Flutter rewrite. Do not commit, push, publish, deploy, create a cloud project, or upload personal data.

First read `START_HERE.md`, `docs/PRODUCT_AND_SOCIAL_DIRECTION_2026-09-13.md`, `.agents/contexts/vigil-product-design-context.md`, and `docs/IMPLEMENTATION_AND_AUDIT_PLAN_2026-09-13.md`. Then read the original product/design/architecture/acceptance files and visually inspect all three `Codex Image 7 Sept 2026, ...png` mockup boards. The new dated files supersede old exclusions of social/accounts and old instructions to remove a separate Focus experience.

Audit the current implementation before editing, preserve all inherited changes, and follow the dated implementation plan in order. VIGIL must keep open-ended Track and a target Focus sprint (45 minutes prominent) as two experiences over one canonical Session/Interval engine. Opening History, Review, Circle, Settings, filters, or preview must never alter the active session; tutorial/preview data must be isolated.

Friends and Close Friends are current scope. Implement mutual friendship, a second mutual Close Friend elevation, per-field sharing controls, sensitive fields private by default, preview-as-viewer, revocation/blocking, a quiet Circle pulse, encouragement, and fair opt-in challenges. Never use a global raw-hours productivity leaderboard. Real cross-phone sharing requires a server-authorized backend; if an approved Firebase configuration is unavailable, build the provider boundary, security model/tests, and a clearly labeled local demo, then state the exact blocker—do not fake cloud completion or place secrets in source.

The app must feel exceptionally premium and cinematic: use the mockups as a quality floor, preserve landscape depth, light/dark atmosphere, timer hierarchy, mint/amber state language, native Android clarity, purposeful motion/haptics, reduced motion, TalkBack, large text, and complete empty/loading/error/offline states. Render and inspect phone-sized states at 360dp and 412dp in both themes. Run tests/typecheck plus real preview checks, keep new evidence separate from inherited claims, and finish with exact run/preview instructions, changed files, remaining unknowns, and confirmation that nothing was committed or pushed.
